const nome = "Gabriel"; // string
const num = 12; // number
const float = 12.34; // number
let nomeAluno; // undefined = existe, mas não tem nenhum valor definido
let sobrenome = null; // nulo = existe, mas o seu valor é nulo/não existe
const booleano = false; // Boolean -> true ou false

const a = [1, 2];
const b = [...a];

console.log(a, b);
b.push(3);
console.log(a, b);
